spring:
  application:
    name: help-center-front
  cloud:
    config:
      name: ${spring.application.name}
      profile: dev
      uri: http://qupconfig.quanyou.com.cn/
      failFast: true
    nacos:
      discovery:
        server-addr: 172.30.3.165:8848
  main:
    allow-bean-definition-overriding: true

logging:
  level:
    root: info
    com.quanyou.qup.xxx.mapper: debug
